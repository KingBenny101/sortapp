# Configuration for Sort App
# Leave values blank to use defaults defined in src/config.py

# Directories
# REQUIRED: unlabeled_dir must be provided
unlabeled_dir: /mnt/e/Important/Whatsapp/27112025/images
# OPTIONAL: dataset_dir - if not provided, app runs in cold start mode
dataset_dir: /home/kingbenny101/code/whatsapp-dataset
output_dir: /mnt/e/Important/Whatsapp/27112025
# data_dir: data (relative to script directory)
# temp_dir: temp (relative to script directory)

# Training settings
max_per_class: 200

# Cold start settings
allow_cold_start: true
min_samples_per_class: 5

# Application settings
app_title: Sort App

# Display settings
display_width: 480
display_height: 360

# Auto mode settings
auto_mode_wait_time: 1.0
skip_auto_mode_wait: true
batch_size: 2056
use_gpu: true

# Copy settings
copy_useless_images: false
